<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Delete Account</title>
    <link rel="stylesheet" href="../css/sidebar.css" />
    <link rel="stylesheet" href="../css/delacc.css" />
    <link rel="stylesheet" href="../css/alert.css" />
    <link rel="stylesheet" href="../css/background.css">
    <link rel="icon" type="image/*" href="https://ik.imagekit.io/FFSD0037/logo.jpeg?updatedAt=1746701257780">
</head>

<body>
    <div class="delete-container">
        <div class="delete-header">
            <h1>Delete Account</h1>
            <p>This action cannot be undone</p>
        </div>
        <% if (msg) { %>
            <div class="alert" role="alert">
              <span class="alert-text"><%= msg %></span>
              <span class="close-btn" onclick="this.parentElement.style.display='none'">&times;</span>
            </div>
          <% } %>
        <div class="warning-text">
            Warning: Your account will be deactivated for 30 days. After that, it will be permanently deleted and cannot be recovered.
        </div>
        <form id="deleteForm" action="/delacc" method="POST" novalidate>
            <div class="form-group">
                <label for="password">Confirm Password</label>
                <input type="password" id="password" name="password" required>
                <span class="password-toggle" id="passwordToggle">👁️</span>
                <div class="error-message">Please enter your current password</div>
            </div>
            <div class="form-group">
                <label for="confirmation">Type "DELETE" to confirm</label>
                <input type="text" id="confirmation" name="confirmation" required>
                <div class="error-message">Please type DELETE to confirm</div>
            </div>
            <div class="checkbox-group">
                <label>
                    <input type="checkbox" id="understand" required>
                    I understand that this action is permanent and cannot be undone
                </label>
            </div>
            <button type="submit" class="delete-btn" disabled>Delete Account</button>
            <a href="/home" style="text-decoration: none; color: black;"><button type="button" class="cancel-bttn">Cancel</button></a>
        </form>
    </div>
    <script src="../js/background.js"></script>
    <script src="../js/delacc.js"></script>
</body>

</html>